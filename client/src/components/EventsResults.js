import React, { Component } from 'react'
// import EventsResultsCard from "./EventsResultsCard"


//eslint-disable-next-line
class EventsResults extends Component{
    //eslint-disable-next-line

        // e.results.performers
        // e.results.performers.id
        // e.results.performers.name
        // e.results.performers.taxonomies.name
            // This is to try to breakdown by categories
        // e.results.performers.type
        // e.results.performers.url


        render(){
       console.log(this.props.results.performers)


        return(
            // this.props.results.events.map((e) => (
            //     <EventsResultsCard
        

                    <div>

                    </div>

            
            )
        
    }
}


export default EventsResults